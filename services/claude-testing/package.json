{
  "name": "@claude-nexus/testing",
  "version": "1.0.0",
  "description": "AI-driven E2E testing service for Claude Nexus Proxy",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:generate": "TEST_MODE=generate vitest run",
    "test:replay": "TEST_MODE=replay vitest run",
    "db:seed": "bun run ./scripts/seed-database.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "pg": "^8.11.5",
    "supertest": "^7.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/supertest": "^6.0.2",
    "vitest": "^1.6.0"
  }
}

